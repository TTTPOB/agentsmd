## Codex Model Mapping

Apply the shared task model routing policy using the model identifiers and delegation controls available in the current Codex environment. Do not assume that Codex uses the same full model identifiers as OpenCode.
